namespace Percent111.ProjectNS.Enemy
{
    // 근거리 적 클래스
    public class MeleeEnemyUnit : EnemyUnit
    {
        // 근거리 적은 기본 EnemyUnit 동작을 그대로 사용
        // 필요 시 CreateStateMachine() 오버라이드하여 근거리 전용 상태 추가 가능
    }
}
