% Core Style

- 들여쓰기 4스페이스, 탭 금지
- 모든 파일 UTF-8(Without BOM)
- 에디터 전용 코드는 `#if UNITY_EDITOR`로 감싸기(가능하면)
- 함수가 1줄만 수행하면 함수로 분리하지 않기
- 불필요한 중첩 null 체크 지양(필요한 곳에만 예외 처리)
- 전역 변수 최소화: 공유 필요가 없으면 지역 변수로 한정
- 상위 객체가 하위 상태를 bool로 들고있지 말 것. 해당 하위 객체가 상태를 관리
- 팝업/대기 로직은 이벤트 기반으로 작성하고, 호출 측이 구독해 후속 처리

